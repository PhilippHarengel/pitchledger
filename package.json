{
  "name": "pitchledger",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "World Cup 2026 betting recommendations — model picks, publicly graded, every match.",
  "scripts": {
    "daily": "tsx src/run.ts daily",
    "snapshot": "tsx src/run.ts snapshot",
    "backfill": "tsx scripts/backfill.ts",
    "backtest": "tsx backtest/backtest.ts",
    "build:site": "tsx src/run.ts build-only",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@vitest/coverage-v8": "^3.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "zod": "^3.24.0"
  }
}
